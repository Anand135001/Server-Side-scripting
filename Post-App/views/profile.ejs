<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen text-white p-10 bg-zinc-900">
        <div class="flex justify-between items-center">
            <h4 class="text-3xl font-light">Hello <%= user.name %> !</h4>
            <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
        </div>
          
        <h4 class="mt-5 px-1">Create your Post :-</h4>
        <form action="/post" method="post">
            <textarea class="resize-none bg-transparent border-2 border-zinc-800 rounded-md outline-none w-1/3 p-5" id=""  placeholder="Wrtie Your Thoughts" name="content"></textarea>
            <input class="px-3 py-1 bg-blue-500 rounded-md block cursor-pointer " value="Submit" type="submit">
        </form>

        <div class="Posts mt-10">
            <h3>Created Posts..</h3>
            <div class="postcontainer mt-2">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 border-2 border-zinc-700 bg-zinc-800 rounded-md mb-2">
                        <h4 class="text-blue-400 mb-1">Written by : <%= user.username %></h4>
                        <p><%= post.content %></p>
    
                        <div class="btns flex justify-between">
                            <a class="text-blue-500 mt-5" href="">Likes</a>
                            <a class="text-zinc-500 mt-5" href="">Edit</a>
                        </div>
                    </div> 
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>